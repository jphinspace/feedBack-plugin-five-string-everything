// Chart Retuner — module entry (plugin.json "scriptType": "module").
// The runtime lives under src/ so it's served by the sandboxed
// /api/plugins/chart_retuner/src/{path} route; see src/main.js.
import './src/main.js';
